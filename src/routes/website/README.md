# WEBSITE — TABLED FOR LATER

All public marketing pages live here (`/website`, `/website/catalog`, `/website/about`,
`/website/trade`, `/website/contact`, `/website/product/$productId`) with their
components under `src/components/website/`.

This section is inactive and not part of the POS workflow. The application entry
point (`/`) is the POS login screen. Do not expand or redesign this section for now.
